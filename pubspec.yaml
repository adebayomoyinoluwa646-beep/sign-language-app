name: sign_language_app
description: Nigerian Sign Language Recognition App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: 0.10.5+5
  tflite_flutter: 0.10.0
  image: ^4.1.7
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/nsl_model.tflite
    - assets/labels.txt